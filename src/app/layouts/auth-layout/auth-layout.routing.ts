import { Routes } from '@angular/router';
import { ESGScoreComponent } from 'src/app/pages/esg-score/esg-score.component';

const routes: Routes = [
    // other routes
    { path: 'esg-score', component: ESGScoreComponent },
  ];
